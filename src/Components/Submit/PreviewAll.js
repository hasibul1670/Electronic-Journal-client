import React from 'react';
const PreviewAll = ({ data, file, url, comment, selectedOption, selectedReviewer })  => {
        return (
          <div>
            <h2>Preview</h2>

         
          </div>
        );
      }


export default PreviewAll;